import React from'react';import{render,screen,fireEvent,act}from'@testing-library/react';import'@testing-library/jest-dom/vitest';import{describe,it,expect,vi}from'vitest';import{App}from'../src/main';
const data:any={sites:[{code:'SAMPLE',name:'샘플',warningAreaCodes:[]}],observations:[{site_code:'SAMPLE',temperature:20}],forecasts:[],warnings:[],radarFrames:[],typhoons:[],status:{state:'normal',rotateSeconds:5,refreshSeconds:30,radarFrameSeconds:1}};
describe('display',()=>{it('renders pages, empty typhoon and keyboard control',()=>{vi.useFakeTimers();render(<App initial={data}/>);expect(screen.getByText('샘플')).toBeInTheDocument();act(()=>vi.advanceTimersByTime(5000));expect(screen.getByText('현재 발표된 태풍 정보 없음')).toBeInTheDocument();fireEvent.keyDown(window,{code:'Space'});fireEvent.keyDown(window,{key:'ArrowLeft'});expect(screen.getByText('샘플')).toBeInTheDocument();vi.useRealTimers()});it('shows no data state',()=>{render(<App initial={{...data,sites:[],status:{...data.status,state:'stale'}}}/>);expect(screen.getByText('수신된 기상자료가 없습니다')).toBeInTheDocument();expect(screen.getByText(/자료 지연/)).toBeInTheDocument()})});
